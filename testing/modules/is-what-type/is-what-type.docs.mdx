---
description: Determines whether `value` is of type string
---

A NodeJS module for determining whether `value` is of type string | number

### Component usage

```ts

const foo = isWhatType('foo');
const oddNumber = isWhatType(15);

console.log(foo); // echo `string`
console.log(oddNumber); // echo `number`
```
